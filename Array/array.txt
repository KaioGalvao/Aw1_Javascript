<!doctype html>
<html>
<head>
    <meta charset="UTF-8"/>
   <script>
	var nome = ["k", "a", "i", "o"];
	window.alert("Array: " + nome);
	
	nome[0] = "*";
	window.alert("Inserindo item na primeira posição: " + nome);

	nome[nome.length-1] = "*";
	window.alert("Inserindo item na ultima posição: " + nome);

	nome.splice(1, 1);
	window.alert("Removendo item na posição 1: " + nome);

	var valores = [10, 20, 30, 1];
	window.alert("Array: " + valores);	

	var soma = 0;
	for(var i = 0; i < valores.length; i++){
		soma = soma + valores[i];
	}

	window.alert("Soma: " + soma);	

	var mult
	mult = valores[3] * 7;

	window.alert("Multiplicação: " + mult);	

    </script>
</head>
<body>
</body>
</html>